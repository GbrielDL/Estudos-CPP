# 🔢 Validador de Número Primo em C++

Este projeto é um **programa simples em C++** que verifica se um número informado pelo usuário é primo ou não, utilizando um menu interativo no terminal.

O objetivo principal é **praticar entrada de dados, estruturas de decisão e laços de repetição**.

---

## 🚀 Funcionalidades

- 🔍 Verificar se um número é primo
- 🔁 Permite validar vários números em sequência
- ❌ Encerrar o programa a qualquer momento

---

## 🧠 Conceitos Utilizados

- Entrada e saída de dados (`cin` / `cout`)
- Estrutura condicional (`if / else`)
- Estrutura de repetição (`do while`)
- Funções auxiliares para leitura de números
- Uso de `setlocale` para suporte ao português

---

## 🛠️ Pré-requisitos

- Compilador **g++**
- C++11 ou superior
- Windows, Linux ou macOS

---

## ▶️ Como Compilar e Executar

### Compilação (exemplo):

```bash
g++ src/main.cpp src/funcoes.cpp -o validador_primo
